apiVersion: v1
kind: Service
metadata:
  name: flex-test-office-booking
  namespace: flex-test-office-booking
spec:
  selector:
    app: flex-test-office-booking
  type: ClusterIP
  ports:
    - name: http
      port: 8082
