apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flex-test-parking-space-pvc
  namespace: flex-test-parking-space
  labels:
    app: flex-test-parking-space
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100M