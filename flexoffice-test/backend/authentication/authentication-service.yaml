apiVersion: v1
kind: Service
metadata:
  name: flex-test-authentication
  namespace: flex-test-authentication
spec:
  selector:
    app: flex-test-authentication
  type: ClusterIP
  ports:
    - name: http
      port: 8081
