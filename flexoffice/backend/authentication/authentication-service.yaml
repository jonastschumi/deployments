apiVersion: v1
kind: Service
metadata:
  name: flex-authentication
  namespace: flex-authentication
spec:
  selector:
    app: flex-authentication
  type: ClusterIP
  ports:
    - name: http
      port: 8081
