apiVersion: v1
kind: Service
metadata:
  name: flex-office-booking
  namespace: flex-office-booking
spec:
  selector:
    app: flex-office-booking
  type: ClusterIP
  ports:
    - name: http
      port: 8082
