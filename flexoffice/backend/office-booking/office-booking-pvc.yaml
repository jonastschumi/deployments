apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flex-office-booking-pvc
  namespace: flex-office-booking
  labels:
    app: flex-office-booking
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100M